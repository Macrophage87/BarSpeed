package com.macrophage.barspeed.ui

import androidx.compose.ui.graphics.Color

/**
 * BarSpeed's semantic palette. The app is deliberately dark-only: it is read
 * at arm's length mid-set, so contrast and one loud accent beat adaptability.
 */
object BarColors {
    val Bg = Color(0xFF0F1115)
    val Surface = Color(0xFF171A21)
    val Text = Color(0xFFE6E8EE)
    val Sub = Color(0xFF8A8F9C)
    val Track = Color(0xFF22262F)
    val Ghost = Color(0xFF3A4150)
    val Volt = Color(0xFFA5F34A)
    val VoltDim = Color(0xFFCBE86B)
    val HeroGreen = Color(0xFF1D2B12)
    val Amber = Color(0xFFFFB74D)
    val Red = Color(0xFFFF6B6B)
    val Blue = Color(0xFF5BA0F4)
}
